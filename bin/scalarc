def time[A](a: => A) = {
   val now = System.nanoTime
   val result = a
   val micros = (System.nanoTime - now) / 1000
   println("%d microseconds".format(micros))
   result
}


val mgr = new FlinkDDFManager()

import scala.collection.JavaConversions._
import io.ddf.flink._

